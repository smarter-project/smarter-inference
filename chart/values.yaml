#

application:
  namespace: smarter
  appName: smarter-admission-controller

image:
  repository: registry.gitlab.com/smarter-project/admission-controller/patch-cleaning
  # @default -- chart.appVersion
  tag: ""
  pullPolicy: IfNotPresent
